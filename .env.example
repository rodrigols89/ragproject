# ============================================================================
# ARQUIVO DE EXEMPLO DE VARIÁVEIS DE AMBIENTE
# ============================================================================
#
# Este arquivo contém todas as variáveis de ambiente necessárias para
# o funcionamento do projeto RAG Project.
#
# INSTRUÇÕES:
# 1. Copie este arquivo para .env na raiz do projeto:
#    cp env.example .env
#
# 2. Preencha os valores apropriados para cada variável
#
# 3. NUNCA commite o arquivo .env no Git (já está no .gitignore)
#
# ============================================================================

# ============================================================================
# CONFIGURAÇÃO DO POSTGRESQL
# ============================================================================

# Nome do banco de dados a ser criado
POSTGRES_DB=easy_rag_db

# Usuário do banco de dados
POSTGRES_USER=easyrag

# Senha do banco de dados
# Use uma senha forte em produção
POSTGRES_PASSWORD=easyragpass

# Nome do serviço (container) do banco no docker-compose
# Em Docker Compose: use 'db' (nome do serviço)
# Em desenvolvimento local: use 'localhost'
POSTGRES_HOST=db

# Porta padrão do PostgreSQL
POSTGRES_PORT=5432

# ============================================================================
# CONFIGURAÇÃO DO REDIS
# ============================================================================

# Nome do serviço (container) do Redis no docker-compose
# Em Docker Compose: use 'redis' (nome do serviço)
# Em desenvolvimento local: use 'localhost'
REDIS_HOST=redis

# Porta padrão do Redis
REDIS_PORT=6379

# ============================================================================
# CONFIGURAÇÃO DO DJANGO
# ============================================================================

# Chave secreta do Django para criptografia e segurança
# Gere uma chave segura usando:
# python -c "from django.core.management.utils import \
#     get_random_secret_key; print(get_random_secret_key())"
# Em produção, use uma chave forte e única
DJANGO_SECRET_KEY=djangopass

# Modo de debug (True/False)
# True = desenvolvimento (mostra erros detalhados)
# False = produção (oculta informações sensíveis)
DJANGO_DEBUG=True

# Hosts permitidos para acessar a aplicação
# '*' = libera para qualquer host (apenas desenvolvimento)
# Em produção: seu-dominio.com,www.seu-dominio.com
# Separe múltiplos hosts por vírgula (sem espaços)
DJANGO_ALLOWED_HOSTS=*

# ============================================================================
# CONFIGURAÇÃO DO UVICORN
# ============================================================================

# Host onde o servidor irá escutar
# 0.0.0.0 = escutar em todas as interfaces (Docker)
# 127.0.0.1 = apenas localhost (desenvolvimento local)
UVICORN_HOST=0.0.0.0

# Porta interna do app Django
UVICORN_PORT=8000

# ============================================================================
# CONFIGURAÇÃO DO CELERY
# ============================================================================

# URL do broker do Celery (onde as tasks serão enfileiradas)
# Formato: redis://host:port/database
# Redis service 'redis' no docker-compose, usando database 0
CELERY_BROKER_URL=redis://redis:6379/0

# URL do backend de resultados do Celery
# Onde o resultado das tasks será guardado
# Usar Redis DB 1 separado é uma prática comum
CELERY_RESULT_BACKEND=redis://redis:6379/1

# ============================================================================
# CONFIGURAÇÕES OPCIONAIS DO CELERY (PARA TESTES)
# ============================================================================

# Executa tasks de forma síncrona (sem fila) quando True
# Útil para testes unitários
CELERY_TASK_ALWAYS_EAGER=False

# Propaga exceções quando tasks são executadas de forma eager
# Útil para debugging em testes
CELERY_TASK_EAGER_PROPAGATES=True

# ============================================================================
# CONFIGURAÇÕES DO SUPERUSUÁRIO INICIAL
# ============================================================================
#
# Estas credenciais são usadas pelo comando 'python manage.py init_setup'
# para criar automaticamente o superusuário inicial do Django.
#
# OPCIONAL: Se não definir, você pode criar manualmente usando:
# python manage.py createsuperuser

# Nome de usuário do superusuário
DJANGO_SUPERUSER_USERNAME=drigols

# Email do superusuário
DJANGO_SUPERUSER_EMAIL=drigols.creative@gmail.com

# Senha do superusuário
# Use uma senha forte em produção
DJANGO_SUPERUSER_PASSWORD=drigols

# ============================================================================
# CONFIGURAÇÕES DE AUTENTICAÇÃO SOCIAL (OAUTH2)
# ============================================================================

# ============================================================================
# GOOGLE OAUTH2
# ============================================================================
#
# Para obter as credenciais:
# 1. Acesse: https://console.cloud.google.com/
# 2. Crie um projeto ou selecione um existente
# 3. Vá em: APIs e serviços → Credenciais
# 4. Crie um ID do cliente OAuth 2.0
# 5. Configure a tela de consentimento OAuth
# 6. Adicione o URI de redirecionamento:
#    http://localhost:8000/accounts/google/login/callback/
#
# Mais detalhes no README.md

# Client ID do Google OAuth2
GOOGLE_CLIENT_ID=seu_google_client_id_aqui

# Client Secret do Google OAuth2
GOOGLE_CLIENT_SECRET=seu_google_client_secret_aqui

# ============================================================================
# GITHUB OAUTH2
# ============================================================================
#
# Para obter as credenciais:
# 1. Acesse: https://github.com/settings/developers
# 2. Clique em: OAuth Apps → New OAuth App
# 3. Preencha:
#    - Application name: Easy RAG
#    - Homepage URL: http://localhost:8000
#    - Authorization callback URL:
#      http://localhost:8000/accounts/github/login/callback/
# 4. Clique em: Register Application
# 5. Copie o Client ID e gere um Client Secret
#
# Mais detalhes no README.md

# Client ID do GitHub OAuth2
GITHUB_CLIENT_ID=seu_github_client_id_aqui

# Client Secret do GitHub OAuth2
GITHUB_CLIENT_SECRET=seu_github_client_secret_aqui

# ============================================================================
# FIM DO ARQUIVO
# ============================================================================

